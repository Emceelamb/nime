void toggleSwitches(unsigned long currentMS) {
  toggleSwitch(sw0, sw0_state, sw0_prev,  currentMS);
  toggleSwitch(sw1, sw1_state, sw1_prev,  currentMS);
  toggleSwitch(sw2, sw2_state, sw2_prev,  currentMS);
  toggleSwitch(sw3, sw3_state, sw3_prev,  currentMS);
  toggleSwitch(sw4, sw4_state, sw4_prev,  currentMS);
  toggleSwitch(sw5, sw5_state, sw5_prev,  currentMS);
  toggleSwitch(sw6, sw6_state, sw6_prev,  currentMS);
  toggleSwitch(sw7, sw7_state, sw7_prev,  currentMS);
  toggleSwitch(sw8, sw8_state, sw8_prev,  currentMS);
  toggleSwitch(sw9, sw9_state, sw9_prev,  currentMS);
}
